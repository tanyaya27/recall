{
 "name": "recall-rules-test",
 "private": true,
 "type": "module",
 "devDependencies": {
  "@firebase/rules-unit-testing": "^4",
  "firebase": "^11",
  "firebase-tools": "^14"
 },
 "scripts": {
  "test": "firebase emulators:exec --only firestore --project recall-test \"node test.mjs && node test_grant.mjs && node test_p2.mjs\"",
  "test:ai": "./run_ai_test.sh"
 }
}
